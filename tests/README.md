# Root test notes

Project-level smoke checks are documented here. Unit tests live close to each application:

- Backend: `backend/tests`
- Frontend: `frontend/src/**/*.test.ts`

Use `docker compose up --build` for the end-to-end local runtime check.
